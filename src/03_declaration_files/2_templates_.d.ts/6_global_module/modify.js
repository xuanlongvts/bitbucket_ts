// var unuse = require("./module");
// or
require("./module");

var xxx = "hello, world";

// Creates new methods on built-in types
console.log(xxx.startsWithHello());

var yyy = [1, 2, 3];
// Creates new methods on built-in types
console.log(yyy.reverseAndSort());
